# CC-SOP Monitor Backend Dependencies
# Python 3.10+

# === Web Framework ===
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
websockets>=11.0

# === Data Validation ===
pydantic>=2.0.0
pydantic-settings>=2.0.0

# === Database ===
sqlalchemy>=2.0.0
asyncpg>=0.28.0
redis>=4.5.0
alembic>=1.12.0

# === Computer Vision ===
opencv-python==4.8.1.78  # 锁定版本，兼容 numpy 1.24.x
numpy==1.24.4            # 锁定版本，兼容 xtcocotools
Pillow>=10.0.0

# === Audio Processing ===
sounddevice>=0.4.6
soundfile>=0.12.0

# === AI/ML - PyTorch & MMPose ===
# 这些包需要特殊安装，请使用安装脚本: ./scripts/install_deps.sh
# 或手动按顺序安装:
#   1. pip install torch==2.1.0+cu121 torchvision==0.16.0+cu121 --index-url https://download.pytorch.org/whl/cu121
#   2. pip install openmim && mim install mmengine==0.10.2
#   3. pip install mmcv==2.1.0 -f https://download.openmmlab.com/mmcv/dist/cu121/torch2.1/index.html
#   4. mim install mmdet==3.2.0
#   5. pip install mmpose==1.3.1 --no-deps && pip install munkres scipy matplotlib json-tricks
#   6. pip install xtcocotools --no-cache-dir --no-binary xtcocotools --no-build-isolation

# === HTTP Client ===
httpx>=0.24.0
aiohttp>=3.8.0
openai>=1.0.0

# === Configuration ===
pyyaml>=6.0
python-dotenv>=1.0.0

# === Logging ===
loguru>=0.7.0

# === Testing ===
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# === Code Quality ===
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0

# === Report Generation ===
reportlab>=4.0.0
jinja2>=3.1.0
